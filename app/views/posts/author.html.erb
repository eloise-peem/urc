<% if @post.errors.any? %>

  <div id="error_explanation">
    <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>
 
    <ul>
    	<% @post.errors.full_messages.each do |msg| %>
      		<li><%= msg %></li>
    	<% end %>
    </ul>
  </div>
<% end %>

<b></b><%= raw author_links(f.authors) %>

<div class = "container">
	<div id = "page_wrapper">
		<br>
			<h2>Add Author(s)</h2>	
		<hr>
		
		<%= form_for @post do |f| %>
		<br>
			<div class="field">
				<th>Authors</th>
			    <br><%= f.text_field :authors, :required => true %> 
			</div>

			<br>
			<div class="actions">
				<%= f.submit "Add" %>
			</div>
			
		<% end %>
	</div>
</div>
